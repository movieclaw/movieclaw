"""网页播放器的服务层（docs/design/web-player.md）。

分工：
- ``hwprobe`` —— 硬件加速可用性判定；
- ``plan``    —— 装配决策输入、调用协议无关的判定引擎、翻译成响应模型。

真正的判定逻辑在 ``movieclaw_playback.decide``（纯函数、零 IO），本包只做
「取数 + 组装 + 翻译」，不夹带任何档位判断——判定散进服务层就没法表驱动单测了。
"""
