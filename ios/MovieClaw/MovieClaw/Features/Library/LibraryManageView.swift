import SwiftUI

// 占位：由对应模块实现（见 docs/design/ios-app.md「模块分工」）。
struct LibraryManageView: View {
    var openCreate: Bool = false
    var initialTab: String?

    var body: some View {
        PlaceholderPage(title: "管理媒体库")
    }
}
