import SwiftUI

struct LibraryItemDetailView: View {
    let libraryId: Int; let itemId: Int; var season: Int?; var episode: Int?
    var body: some View { PlaceholderPage(title: "LibraryItemDetailView") }
}
