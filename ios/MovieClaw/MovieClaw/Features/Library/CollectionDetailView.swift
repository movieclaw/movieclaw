import SwiftUI

// 占位：由对应模块实现（见 docs/design/ios-app.md「模块分工」）。
struct CollectionDetailView: View {
    let libraryId: Int?
    let collectionId: Int

    var body: some View {
        PlaceholderPage(title: "合集")
    }
}
