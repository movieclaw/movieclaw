import SwiftUI

// 占位：由对应模块实现（见 docs/design/ios-app.md「模块分工」）。
struct SubscriptionDetailView: View {
    let subscriptionId: Int
    var openUpgradeRun: Bool = false

    var body: some View {
        PlaceholderPage(title: "订阅详情")
    }
}
