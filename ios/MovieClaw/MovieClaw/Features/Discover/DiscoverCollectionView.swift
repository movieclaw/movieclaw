import SwiftUI

// 占位：由对应模块实现（见 docs/design/ios-app.md「模块分工」）。
struct DiscoverCollectionView: View {
    let kind: String
    let provider: String
    let collectionId: String

    var body: some View {
        PlaceholderPage(title: "榜单")
    }
}
