import SwiftUI

// 占位：由对应模块实现（见 docs/design/ios-app.md「模块分工」）。
struct SearchResultsView: View {
    let query: AppRoute.SearchQuery

    var body: some View {
        PlaceholderPage(title: "搜索结果")
    }
}
