# 刮削与整理个性化：配置菜单设计

> 状态：草案（待评审）。本文把 media_scrape / movieclaw_media.library /
> library.organize 里写死的选图、语言、命名细节开放为面向收藏玩家的
> 配置菜单，并划清"开放什么、不开放什么"的边界。

## 0. 定位与目标

现状（见 `media_scrape.py`、`movieclaw_media/library.py`、
`library/organize.py`）：刮削与整理走"一套固定最佳实践"，可配置面只有
TMDB 环境变量（语言/地区/图片档位）、库级 `write_media_assets` 开关和
条目级手动选图锁。选图策略、命名模板、分级优先级全部写死。

收藏玩家的诉求是**细节可控**：海报要不要中文字、背景图选哪种、文件名里
带不带分辨率与发布组、目录用中文名还是原名。目标：

1. **一个配置菜单管全部刮削细节**——设置页新增「刮削与整理」分区，
   语言、选图、图片质量、命名模板、目录写入集中在此；
2. **约定优于配置**——每个新配置的默认值就是当前写死的行为，升级后
   不配置则一切照旧，零迁移零惊吓（moviebot 稻草人配置的反面教训依旧
   有效：不预留存而不用的配置，本文开放的每一项都有明确消费者）；
3. **改完能生效**——配置变更后引导用户一键整库刷新/重新整理，而不是
   "改了但存量不动"的半吊子。

非目标：多刮削源（豆瓣详情/Fanart.tv/TVDB）不在本文范围——那是数据源
架构议题，仅在 §5 留接口开口；NFO 内容结构不开放（Kodi/Emby 硬规范）。

## 1. 配置分层

```
代码默认值（= 当前行为）
  └─ 全局「刮削与整理」设置（SettingStore，namespace metadata.scrape）
       └─ 库级覆盖（Library 行内 JSON，只覆盖显式设置的字段）
            └─ 条目级手动锁（现有选图锁，粒度最细、优先级最高）
```

- **全局设置**走现有 `SettingStore` 通用表 + 注册表机制（`settings/base.py`），
  新增配置域零数据库迁移；
- **库级覆盖**加在 `library` 表一个可空 JSON 列 `scrape_overrides`（一次
  迁移）：动漫库要日文原名海报、纪录片库要无字背景，这类"按库口味"
  是收藏玩家的真实场景。空对象 = 全跟全局。只允许覆盖 §2 中标注
  〔可库级〕的字段——命名模板允许库级覆盖（电影库与剧集库模板本就
  独立），语言不允许（语言是条目档案属性，同一条目跨库共享一份档案，
  按库切语言会让两库抢写同一行 media_metadata）；
- **环境变量兼容**：现有 `TMDB_LANGUAGE` 等 env 保留为**部署级初始值**
  ——设置域首次读取（库里无该 namespace 行）时以 env 值为默认落库，
  之后以数据库为准，env 仅在设置从未保存过时生效。`get_settings()` 的
  tmdb_* 读取点（media_scrape、media_discover、image_variants）统一改经
  新的 `get_scrape_config()` 异步读取器（带进程内缓存，settings 保存时失效）。

## 2. 配置面清单（即菜单结构）

### 2.1 语言与地区

| 配置 | 类型/默认 | 替代的写死点 |
|---|---|---|
| 元数据语言优先级 | 有序列表（1~3 项），默认 `[zh-CN, en-US]` | `TMDB_LANGUAGE` + `_fetch_english_text` 写死的 en-US 兜底 |
| 分级优先地区 | 有序多选，默认 `[CN, US]` | `_parse_certification` 写死 CN→US |

`TMDB_REGION`（地区）**不进本分区、也不进设置页**（用户决策
2026-08-27）：它只消费于发现页的「正在热映/即将上映」两行与电影筛选
（`movieclaw_media/service.py` 的 `region` 参数），是"发现页的院线视角
站在哪个国家"，与刮削无关。它**就地放在发现页底部**，形态照 Google 的地区切换：
页脚一行浅色小字「院线地区：中国大陆」，地区名可点击，弹出紧凑的
地区菜单（当前项打 ✓），选择即自动保存、无保存按钮，改完同屏立刻
看到「正在热映/即将上映」刷新——不做带说明文案的控件条，页脚
存在感要低到不打扰浏览。归类原则：按配置的**消费方**
归类而非数据来源，且单字段配置优先做进它影响的页面本身，不为它建
设置分区。存储仍走 `SettingStore`（`discover.preferences` 域）。

**语言做优先级而不是单选**（用户决策 2026-08-27）：Emby/Plex/Kodi 的
心智模型都是"首选语言 + 回落"，单选 + 独立的"简介兜底语言"是同一
语义的拆散表达，合并成一个有序列表更直观，也与选图的语言优先级（§2.2）
同构。语义与成本：

- **首位 = 请求语言**：TMDB 单次请求只接受一个 `language`，首位决定
  详情/季集请求的语言，也决定类型名、演员译名等只随请求语言走的字段；
- **条目级字段回落零额外请求**：档案请求本就带
  `append_to_response=translations`（现只用于收集别名），标题/简介/
  tagline 的各语言翻译已在同一响应里——主语言缺失时按优先级从
  translations 本地取下一语言即可。现状"简介缺失补拉一次 en-US"
  是它的退化版，改造后那次请求反而省掉；
- **"缺失"按字段判定**：简介/tagline 看空值；标题看 translations 里
  有无该语言的翻译条目（TMDB 对无翻译的标题会自动退回原名，不能拿
  返回值判断）。默认 `[zh-CN, en-US]` 下与现状行为一致：简介兜底
  语义不变，标题回落与 TMDB 自身的回落链等效；
- **季集级回落有闸**：分集标题/简介不在 translations 里，逐集拉翻译
  是每集一次请求，不可接受。规则：某季主语言的分集覆盖率过低（集名
  多为 "Episode N" 占位/简介大面积为空）时，整季按下一语言**重拉一次**
  ——每季每级至多一次请求，且只在刮削管线内生效；
- `media_metadata.scrape_language` 记录首位语言（现状语义不变）。

不开放：别名收集范围（`_ALIAS_REGIONS/_ALIAS_LANGUAGES`）。别名喂的是
匹配内核（种子命名以中英为主），不是展示偏好；开放它只会让用户误伤
自己的识别率。

### 2.2 选图偏好〔可库级〕

**图片语言与文本语言同构**（用户决策 2026-08-27）：TMDB 每张候选图带
`iso_639_1`（null = 无文字烧录），图片的语言选择用与元数据语言完全一致的
**优先级列表**机制，不做散装单选。列表项除具体语言外含三个特殊项：
「跟随元数据主语言」（引用 §2.1 首位，改语言时选图自动跟随）、
「原始语言」（按条目的 original_language 动态解析）、「无文字」（null）。

| 配置 | 类型/默认 | 说明 |
|---|---|---|
| 海报选择 | 二选一，默认「TMDB 默认（与发现页一致）」 | 另一档「按语言优先级挑选」——展开下面的优先级列表；选后接受"订阅前后海报可能跳变"（菜单内注明）。原草案的「本地化优先/原始语言/无文字」三个单选只是该列表的三种预设排序，统一进列表 |
| 海报语言优先级 | 有序列表，默认 `[跟随元数据主语言, en, 无文字]` | 逐级取第一档有候选的语言，档内按分辨率门槛 + 加权票数排序（`_sorted_candidates` 复用） |
| 背景图语言优先级 | 有序列表，默认 `[无文字, 跟随元数据主语言, en]` | 同一机制；"无文字优先"即默认首位，想要带 logo 横图的把语言排到前面即可，原草案的「按票数」单选被列表机制覆盖 |
| 最低分辨率门槛 | 海报/背景两个数字，默认 500/1920 | `_MIN_POSTER_WIDTH` / `_MIN_BACKDROP_WIDTH`；0 = 不设门槛 |
| 图片质量档位 | 海报/背景/剧照三个下拉，默认 w780/original/w300 | `TMDB_POSTER_SIZE` 等三个 env 入菜单；选项列 TMDB 合法档位，注明磁盘占用量级 |

实现注意：候选图请求的 `include_image_language` 须由优先级列表推导
（列表中出现的语言 + null），而不是现在写死的 `null,{语言},en`；
「更换图片」弹层的候选排序（`list_image_candidates`）与自动选图共用
同一份优先级参数，保持"列表第一张 = 自动会选的那张"的既有承诺。

评分公式（`_weighted_score`）不开放——它是防"1 票 10 分"的正确性
机制，不是口味。手动选图锁不变，仍是最高优先级。

### 2.3 命名模板〔可库级〕

**机制选择：受控 token 模板，否决自由模板引擎**（权衡见 §5）。

四个模板（前两个电影库用，后两个剧集库用；花括号 token 渲染后逐段
`sanitize_folder_name`，token 之外的字面文本原样保留）：

| 模板 | 默认值（= 现状） |
|---|---|
| 条目目录 | `{title} ({year})` |
| 电影文件名 | `{title} ({year})` |
| 季目录 | `Season {season:02d}` |
| 剧集文件名 | `{title} ({year}) - S{season:02d}E{episode:02d}` |

可用 token：`title` / `original_title` / `year` / `tmdb_id` / `imdb_id` /
`season` / `episode` / `episode_title` / `resolution` / `media_source` /
`release_group`。数字 token 支持 `:02d` 补零后缀，其余不引入条件/过滤器
语法——需要"字段缺失时不留空括号"的场景由渲染器统一处理（token 为空
时连同紧邻的成对括号与分隔符收缩，规则确定性、有单测）。

校验（保存时前置报错，中文文案）：

- 条目目录模板必须含 `{title}` 或 `{original_title}`；剧集文件名模板必须
  含 `{season}` 与 `{episode}`（否则多集同名互相覆盖）；
- token 渲染值内的 `/` 等保留字符照走 `sanitize_folder_name`，模板字面
  文本中禁止路径分隔符（模板只描述**一段**目录/文件名，层级结构固定为
  `条目目录[/季目录]/文件名`，不开放自定义层级——`entry_dirs` 识别链、
  条目删除、NFO 落点都依赖这个结构假设）;
- 保存前渲染一部内置示例电影 + 一部示例剧集实时预览。

**命名同源约束**（本节的实现红线）：`标题 (年份)` 目前四处共用——
`derive_save_path`（投递 save_path）、`ingest`（入库落名）、`organize`
（整理目标）、`entry_base_name`（镜像/删除的条目目录判定）。模板化必须
收敛到一个新模块 `library/naming.py` 统一渲染，四处全部改读它，**不允许
任何一处继续拼字符串**——否则改了模板后投递落 A 名、整理算 B 名，
strm 回流链路（docs/design/strm-workflow.md）直接断裂。

版本标签（` - 标签` 后缀，`_version_labels` 的分辨率→片源→发布组
递进）不模板化：它是同名冲突时的**追加逻辑**而非命名偏好，且必须保持
确定性幂等。用户想在常规文件名里带分辨率，用 `{resolution}` token 即可。

**模板变更的存量语义**：整理器本就是"算规范名→不一致则改名"的幂等
引擎，模板变更后存量自然全部"待整理"。保存模板时提示"对存量生效
需对各库执行一次整理"，并给一键入口；不自动触发（大库改名是重操作，
用户该看着整理预览确认）。

### 2.4 媒体目录写入〔可库级〕

现有单开关 `write_media_assets` 拆细：

| 配置 | 默认 | 说明 |
|---|---|---|
| 写入图片（poster/fanart/剧照） | 开 | 现状行为 |
| 写入 NFO | 开 | 有人用 Emby 自己刮、只要我们的图 |
| 分集剧照镜像 | 开 | 几百集的剧照是最大的写入量，单独可关 |

`write_media_assets` 列保留为总闸（关 = 三项全关），三个细项进
`scrape_overrides`；镜像的"只增不覆盖不删除 + 资产较新才覆盖"铁律
不开放配置。

### 2.5 明确不开放的

- 刷新分档周期（`next_refresh_delay`）：调快就是打爆 TMDB 限流，
  收藏玩家的诉求里也没有它；
- NFO 文件名与内容结构、镜像图片文件名（Kodi/Emby/Jellyfin 硬规范，
  改了播放器不认）；
- 演员数上限、加权评分公式、原盘目录处理。

## 3. 交互设计

- 设置页新增「刮削与整理」分区（`apps/web` settings-view 加 section，
  与现有分区同构）。分区内按"配置的是什么"分四个 tab（用户决策
  2026-08-27）：**元数据**（语言优先级、分级优先地区）、**图片**（海报/
  背景语言优先级、门槛、质量档位）、**命名与整理**（模板 + 实时预览）、
  **目录写入**（镜像开关）。排列顺序沿用刮削管线的先后，只为读起来顺；
  **四组之间没有依赖，可任意顺序配置，因此不编号**（用户决策 2026-08-28：
  初版给 tab 打了 STEP 1~4，被指出"它们没有步骤依赖关系"——编号会把并列
  分组伪装成必须按序完成的向导，是拿结构装饰内容，已移除）；
- 所有"有序优先级"的交互统一为**排序芯片**（用户决策 2026-08-27）：
  一行候选芯片，点击加入优先级并按点击顺序编号（首位标「主」/「首选」），
  再次点击移除；不用"列表行 + 上下箭头 + 添加下拉"（对 2~3 项的排序
  过重）。元数据语言、海报语言、背景语言、分级优先地区四处共用同一
  组件；至少保留一项（主语言不可清空）；
- 语种候选**不写死**（用户决策 2026-08-27）：芯片行只放常用语种快捷项，
  末尾「+ 更多…」展开可搜索的完整列表——语言全量来自 TMDB
  `configuration/languages` 接口（约 180 种，后端代理并缓存），地区全量
  来自 `configuration/countries`；从完整列表选中的语种加入芯片行并
  参与排序，已保存的非常用语种始终显示在行内；
- 图片语言的「原始语言」特殊项须配说明（tooltip + 文案）：它是 TMDB
  `original_language`——作品的**原声语言**（《寄生虫》为韩语、日本动画
  为日语），随条目动态解析，不是"发行地语言"；「跟随元数据主语言」
  同样注明引用的是「元数据」tab 里语言优先级的第 1 位；
- 「地区」不在本分区（见 §2.1）——就地内联在发现页底部，选择即保存；
- 命名模板卡片：模板输入 + token 快捷按钮 + 双示例实时预览 +
  「恢复默认」；保存后弹条提示"存量文件需整理后生效"带各库整理入口；
- 选图偏好卡片保存后提示"存量图片需整库刷新生效"带刷新入口
  （刷新本就按新配置全量重选重下，`sources.json` 溯源机制已支持）；
- 库编辑弹窗加「高级：覆盖全局刮削设置」折叠区，仅列〔可库级〕字段，
  每字段三态：跟随全局 / 显式值。

## 4. 代码落点

| 改动 | 位置 |
|---|---|
| 新配置域 `MetadataScrapeSetting`（namespace `metadata.scrape`） | `settings/metadata.py` 新文件，含 env 播种逻辑 |
| 读取器 `get_scrape_config()`（全局+库级合并，带缓存） | 同上 |
| 命名渲染器（模板解析/校验/渲染/示例预览） | `services/library/naming.py` 新文件 |
| 四处命名接入改造 | `library/config.py`（derive_entry_dir）、`layout.py`（entry_base_name）、`ingest.py`、`organize.py` |
| 选图策略接入偏好参数 | `movieclaw_media/library.py`（pick_poster/pick_backdrop/list_image_candidates 加参数，保持纯函数） |
| 语言优先级/分级接入（translations 本地回落 + 季级重拉闸，替代 `_fetch_english_text`） | `movieclaw_media/library.py`、`media_scrape.py` |
| `library.scrape_overrides` 列 | alembic 迁移一次 |
| 设置 API + 前端分区 | `api/routes/settings*`、`apps/web` |

测试口径：命名渲染器全分支单测（token 缺失收缩、补零、非法模板拒绝、
默认模板渲染结果与现状字节级一致）；选图策略参数化后原有单测跑默认
参数必须零 diff——**"默认值 = 现状"是本设计最重要的可验证承诺**。

## 5. 权衡与已否决方案

- **Jinja2/自由表达式模板**：能力上限高，但错误信息对非开发者不可读、
  安全面大（沙箱逃逸/无限循环）、且条件语法会让"确定性幂等"难以
  静态保证。受控 token + 收缩规则覆盖 MoviePilot/TMM 用户的绝大多数
  真实模板，超出部分（按分辨率分目录、自定义层级）明确拒绝；
- **条目级命名覆盖**：破坏整理幂等（每条目一套规则，改名结果不可
  预测），且条目级诉求实质是"这部片名字刮错了"——那是识别纠错
  （人工认领）的职责，不是命名配置；
- **多刮削源**：不做，但 §2.2 的选图策略函数保持纯函数、以候选列表为
  输入，未来接 Fanart.tv 只是多一路候选来源，策略与菜单不用动；
- **全局设置直接塞 `Settings`（env 体系）**：否决——收藏玩家的调参
  需要界面即时生效，env 改完要重启容器，与"简单、可调配"背道而驰。

## 6. 分期实施

- **P1 设置域与选图/语言**：`MetadataScrapeSetting` + env 播种 +
  语言/分级/兜底/选图/图片档位接入 + 设置页新分区（不含命名模板、
  不含库级覆盖）。价值即时：换语言、换选图口味、调图片质量全部
  界面化，且无迁移。
- **P2 命名模板**：`naming.py` 渲染器 + 四处接入 + 模板卡片与整理
  联动。含一次"默认模板 = 现状"字节级回归验收。
- **P3 库级覆盖**：`scrape_overrides` 迁移 + 合并读取 + 库编辑弹窗
  折叠区 + 目录写入细分开关。

每期独立可发版，P1 不依赖后两期的任何结构。

## 7. P1 实施记录（2026-08-27，与草案的偏差）

P1 已落地（语言优先级 + 选图偏好 + 图片档位 + 设置分区 + 发现页地区），
与草案的偏差与补充如下：

1. **env 播种改为"读取期合并"**：草案设想"设置域首次读取时把 env 值
   落库"，实现改为空值/空列表表示"跟随环境变量"，由
   `services/scrape_config.py` 的 `effective_*` 在读取期合并
   （与 `network_egress` 的镜像地址覆盖完全同构）。好处是不写入用户从未
   设置过的值：用户改 env 后行为立刻跟着变，而不是被首次启动的快照钉死。
2. **`_fetch_english_text` 已删除**：其职责由 translations 本地回落覆盖，
   默认 `[zh-CN, en-US]` 下语义不变但**少一次请求**。
3. **`orig`（原始语言）需要一次补拉**：请求详情前无从得知条目的
   original_language，若它不在 `include_image_language` 里，详情回来后由
   `_ensure_original_language_images` 补拉一次 `/images` 合并候选——只有
   显式配置 `orig` 的用户会走到，失败静默按无候选处理。
4. **季级回落的判据**：`_season_text_poor`——≥60% 分集名为空或匹配
   「第 N 集 / Episode N」占位式即判定覆盖率过低，整季按次位语言重拉一次。
5. **选图函数签名变化**：`pick_poster/pick_backdrop/list_image_candidates`
   由"语言字符串"改为"语言档位列表（None=无文字）+ 门槛"，仍是纯函数。
   `pick_backdrop` 的默认档位 `(None,)` 即历史行为。
6. **`MediaLibraryService(language=...)` 改为 `languages=...`**，默认 None
   表示跟随设置快照；显式传入仅供测试固定语言。
7. **设置页 tab 首期只出「元数据」「图片」两组**：命名与整理、目录写入
   随 P2/P3 接入，tab 容器已就位。（tab 初版带 STEP 1~4 编号，2026-08-28
   按用户意见移除——四组并列无依赖，见 §3。）

验收：`tests/media/test_media_library.py` 覆盖语言回落（含"零额外请求"
断言）、季级回落请求次数、选图分档、`orig` 补拉、分级优先级、token 解析；
`tests/api/test_scrape_settings.py` 覆盖读写、校验拒绝、env 回落与地区
联动。既有用例在默认参数下零 diff（"默认值 = 现状"承诺）。

## 8. P2 实施记录（2026-08-27，命名模板）

四处命名调用点已全部收敛到 `services/library/naming.py`，与草案的偏差：

1. **`entry_base_name` 已删除**：模板化后它只是 `naming.entry_dir_name_of`
   的同义转发——给同一个概念留两个名字正是「命名同源」要防的漂移，
   随调用点收敛一并移除（原调用方 organize 改直接读 naming）。
2. **投递侧需要条目上下文**（实现中发现的真缺口）：`derive_save_path`
   原本只接 `title`/`year`，模板里写 `{original_title}` / `{tmdb_id}` 时
   投递侧会渲染成空、与整理侧算出**不同的目录名**——命名同源当场破。
   修法：`derive_entry_dir` / `derive_save_path` / `resolve_save_path`
   新增可选 `item` 参数，订阅投递（dispatch）、手动下载（已识别条目）、
   监听导入与入库四处全部透传。
   **已知且可接受的降级**：手动下载只输了片名、还没有 TMDB 身份时无
   条目可传，条目专属占位符渲染为空并被收缩（不留 `[tmdbid-]` 残缺文本）；
   该文件落盘后由扫描识别、整理给出规范名。此行为有专门用例钉住。
3. **收缩规则比草案更强**：草案只说"空括号收缩"，实测不够——
   `[tmdbid-{tmdb_id}]` 在缺值时会残留 `[tmdbid-]`（组内 `tmdbid-` 是
   字面文本），而这正是 Emby/Jellyfin 最经典的目录写法。改为**结构化
   收缩**：占位符全空的括号组连同组内字面文本整组丢弃；另加一道括号
   内侧收缩（`[2160p ]` → `[2160p]`）。
4. **季目录模板必须含 `{season}`**（草案未列）：否则不同季的同集号文件
   落进同一目录互相覆盖，与剧集文件名缺季集号是同一类事故。
5. **模板留空 = 用默认**：设置里四个字段默认空串，`effective_templates()`
   逐字段回落到内置默认——用户只想改剧集文件名时不必把四个都填一遍。
6. **前端预览是镜像实现**：实时预览需要逐键渲染，走服务端会有往返延迟，
   故在组件内镜像了同一套三步规则，两侧都写了「改一处必须同步改另一处」
   的交叉注释。落盘名字与保存校验一律以后端为准；本次改动后做了一次
   13 例的前后端逐字对拍（含空括号组、括号内侧、补零、保留字符清洗）。

验收：`tests/api/test_library_naming.py` 38 例——含 6 组**默认模板与旧实现
逐字节一致**的回归（条目目录/季目录/剧集文件名/电影文件名 × 含冒号、
斜杠、无年份、三位集号等边界）、收缩规则、校验拒绝、四处调用点一致性、
无条目降级；`tests/api/test_scrape_settings.py` 覆盖模板落库、生效与
422 拒绝。既有的整理/入库/投递用例（146 例）零改动全过。

## 9. P3 实施记录（2026-08-27，库级覆盖）

`library.scrape_overrides`（可空 JSON 列，一次迁移 `c8e3fa5b7d12`）落地，
合并读取在 `services/scrape_config.merge_for_library`。与草案的偏差：

1. **〔可库级〕的范围被收窄了——草案标错了**（实现期修正）。判据只有
   一条：*这个设置的产物是不是按库各存一份*。
   - **命名模板**：产物是路径，落在各库自己的目录树 → 可库级 ✅
   - **目录写入细项**：产物是写进各库目录的图片/NFO 文件 → 可库级 ✅
   - **选图偏好与图片档位**：❌ **不可库级**。`poster_path` /
     `backdrop_path` 存在 `media_item` 上、图片资产按条目 id 存**一份**，
     跨库共享。而"同一部剧的集分散在两个库"是本项目明确支持的常态
     （见 `cleanup_orphan_items` 的注释），两个库配了不同选图偏好就会
     轮流覆盖同一行、同一个文件。这与 §1 给"语言不可库级"列的理由
     完全同构，草案把选图标成〔可库级〕是漏看了数据模型。
   最终可覆盖集合 = 4 个命名模板 + 3 个镜像细项，由
   `scrape_config.LIBRARY_OVERRIDABLE` 单点声明，保存入口按它拒绝越界字段
   （中文 400：「这些设置不支持按库覆盖：…（选图与语言的产物跨库共享
   一份，只能在全局设置里改）」）。
2. **覆盖值走与全局同一套校验**：库设置里写了坏模板会当场 400，而不是
   静默丢弃——"保存成功但没生效"比报错难查得多。读取端另有一层兜底：
   校验失败只记告警并退回全局，脏数据绝不能让整库刮削崩掉。
3. **三态语义**：`scrape_overrides` 不传 = 不改动；空对象 = 显式清空
   （回到全跟全局）；只写要覆盖的字段。界面上每个字段是「跟随全局 /
   显式值」的三态控件（命名模板留空即跟随，镜像开关是三档按钮）。
4. **镜像开关的层级**：库上的 `write_media_assets` 保留为**总闸**（关掉
   三项全关），三个细项在其下生效——既不破坏既有语义，也不用迁移那一列。
   `mirror_media_dir_assets` 内部把条目目录映射到所属库后逐库取开关
   （库根不允许重叠，一个条目目录只归属一个库，这个前提由建库校验保证）。

验收：`tests/api/test_scrape_settings.py` 新增 3 例（覆盖往返与合并生效、
越界字段与非法模板双双 400、空对象清空）；命名/整理/入库/投递/CLI 共
322 例全过。

## 10. 端到端有效性验证（2026-08-27）

`tests/api/test_scrape_config_e2e.py`（28 例）只问一件事：**配置改了，
磁盘上/库里的结果真的跟着变了吗**。与既有测试分工——
`test_scrape_settings.py` 管接口读写与校验、`test_library_naming.py` 管
渲染器纯函数——这里一律跑真实链路：真实建库、真实文件落盘、真实
`organize_library` 改名、真实 `mirror_media_dir_assets` 写目录、真实
`fetch_media_profile` 解析（TMDB 走 MockTransport，不出网），断言落在
**磁盘路径与落库字段**上。接口回显对了而落盘没变，正是这类配置功能
最典型的假通过。

覆盖面：
- **A 命名（7 例）**：默认模板产出与模板化前一致；全局模板改变磁盘布局
  且台账同步；电影目录名与文件名各走各的模板；两个库不同模板各按自己
  落盘；改模板后一次整理到位且再跑幂等；字幕随改名；投递 save_path
  与整理目标逐字同源。
- **B 目录写入（6 例）**：三项全开写齐 poster/fanart/季海报/两级 NFO/
  分集 thumb；分别关掉图片、NFO、分集剧照时磁盘上到底少了哪些文件；
  库总闸 `write_media_assets=false` 三项全封；库级单独关 NFO。
- **C 刮削链路（9 例）**：`poster_mode` 两档的落库 poster_path 不同；
  海报语言优先级换序改变选中项（同一份候选，只有配置变了）；「原始
  语言」按条目解析；背景无文字优先 vs 语言优先；分辨率门槛真的过滤；
  语言优先级改变请求语言且回落零额外请求；分级地区换序改变 rating；
  `include_image_language` 由偏好推导。
- **D 档位与资产（2 例）**：档位改了下载 URL 真的变（含换档位后重下）；
  留空跟随 env、显式值压过 env。
- **E 防假通过（4 例）**：覆盖不在库之间串味；脏覆盖退回全局且整理
  照常跑完；库行里残留的不可覆盖字段静默忽略且不影响全局选图。

**变异验证**：写完后逐一把产品代码打断，确认这些用例会红——整理忽略
命名模板（2 例红）、镜像忽略 NFO 开关（2 例红）、合并读取忽略库级覆盖
（4 例红）、选图忽略语言优先级（4 例红）、语言回落丢掉 translations
（1 例红）。五处变异全部被捕获，测试确有牙齿。

顺带修掉一个实现缺口：`{resolution}` / `{media_source}` /
`{release_group}` 此前**声明为可用占位符却没人供值**——它们在
`LibraryFile` 行（整理侧）与 `probe/enrich` 结果（入库侧）上。现已两侧
都喂进渲染器；入库侧为此把逐文件探测提到命名之前（纯读取，无副作用）。

## 11. 默认值一致性审计与展示侧命名同源（2026-08-28）

### 11.1 默认值逐项对拍：与模板化前逐字节一致

用户问的是最要命的那种细节——「影片名和年份括号前有没有空格」。答案是
**有**：改动前的实现就是 `f"{base} ({item.year})"`，新的默认模板
`{title} ({year})` 渲染出的是同一个字符串。

不靠肉眼比对，做了两轮机器对拍：

- **命名（1368 组合）**：把改动前提交里的 `entry_base_name` /
  `derive_entry_dir` / 整理目标拼接**原样抄出来**，与新的默认模板逐
  组合比对——18 个标题（含 `:`、`/`、`?` 等非法字符、首尾空格、结尾点
  号、空标题）× 4 个年份（含 None）× 6 个季 × 6 个集 × 2 个扩展名，
  覆盖条目目录名、`derive_entry_dir`、电影完整目标路径、剧集完整目标
  路径四个产出。**零差异**。
- **其余 13 项默认值**：语言优先级（跟随 `TMDB_LANGUAGE` + `en-US`
  兜底 = 改动前"主语言请求 + 英文兜底"）、分级地区 `["CN","US"]`、
  `poster_mode=default`、海报/背景语言优先级、宽度门槛 500/1920、
  三个档位留空跟随 env（`w780`/`original`/`w300`）、发现页地区跟随
  `TMDB_REGION`、三个镜像开关默认全开、四个命名模板默认留空。
  逐项与改动前的写死值/env 默认值对拍，**全部一致**。

结论：**新装或从旧版本升级、什么都不配的用户，行为与升级前完全相同。**
这两个对拍脚本是一次性验证工具（对拍的是"已经不存在的旧实现"），不进
测试套件；进套件的是 §8 里那条常驻的默认模板回归。

### 11.2 审计副产品：四处前端自己拼「标题 (年份)」

对拍时顺手搜了全仓还有谁在独立构造条目目录名，捞出**四个前端展示点**
仍写死 `${title} (${year})`：

| 位置 | 文案 |
|------|------|
| `download-target-dialog.tsx` | 手动下载弹窗「直接下载到 …」 |
| `subscribe-dialog.tsx` | 订阅弹窗「将直接下载到库内目录 …」 |
| `subscription-adjust-dialog.tsx` | 调整订阅「将直接下载到库内目录 …」（只显示库主根，更粗） |
| `subscription-settings-section.tsx` | 订阅设定「模拟一单」第 2 步 |

它们**不写盘**（真实落点始终由后端 `derive_save_path` 决定），所以不会
造成数据错乱；但用户一旦改了命名模板，预览就与真实落点对不上——而"预览
即真实"正是这四个弹窗存在的理由。这是命名同源在展示侧的同一条红线。

修法与后端同源规则一致：**谁都不许自己拼，名字只有一个来源**。
`preview_dispatch_route` 增加展示用的 `entry_dir` 字段（按生效模板渲染
出的条目目录完整路径），四处前端改为直接显示它。要点：

- `entry_dir` **只影响展示**，不参与 `mode`/`path` 推导——预检给出的
  仍是投递基底目录，条目目录到投递时才推导（原有语义不动）；
- 标题取值：**已建档条目的权威标题优先**，其次才是调用方传入的识别
  标题。手动下载弹窗传的是种子标题（`Zootopia.2016.1080p`），真实投递
  却是按 TMDB 身份建档后推导的（`疯狂动物城 (2016)`）——这一条顺带
  修掉了一处早于本次改动就存在的预览失真；
- 显式选库（不走路由）时也补一次轻量条目查询，好让
  `{original_title}`/`{tmdb_id}` 这类占位符在预览里也渲染得出；
- `RouteDecision` 带上 `item`，路由结论与条目一并回给调用方。

回归锚：`tests/api/test_save_path_decision.py` 三例——默认模板下
`entry_dir` 与写死行为一致、换模板后预览跟着变（证明走的是模板）、
没有身份时不臆造目录（为空）。

## 12. 反复调整模板不留垃圾（2026-08-28）

用户提的产品要求是硬的:**「要支持反复的调整重新整理,不能残留任何垃圾
信息」**。反复改模板试效果是本功能的预期用法,不是边角场景。

### 12.1 实测出来的漏洞

改 `naming_entry_dir` 后再整理一次,实测现场:

```
旧目录还在吗: True
旧目录剩下: ['fanart.jpg', 'movie.nfo', 'poster.jpg', '片 (2020)-thumb.jpg']
新目录内容: ['片 (2020).mkv', '片 (2020).nfo']
removed_dirs: 0
```

两条规则撞在一起:

1. 整理器的附属文件判定是「同目录 + 以 **主文件名.** 开头」。镜像写出的
   `poster.jpg` / `fanart.jpg` / `seasonNN-poster.jpg` / `movie.nfo` /
   `tvshow.nfo` 都不满足;分集剧照 `xxx-thumb.jpg` 是 `-thumb` 不是 `.`,
   同样漏掉;
2. 镜像模块是**只增不删**的铁律,旧目录那份永远不会被清掉。

后果:旧条目目录非空 → `_prune_emptied_dirs` 的 rmdir 全部失败 → **每调
一次模板就多留一层只剩图片的空壳目录**。

### 12.2 修法

不新增"清理"能力,而是让**改名把自己的产物带走**——搬空了,现有的空目录
清理自然收干净,「绝不删用户文件」的保证不破。

- **附属文件前缀放宽**:`主文件名.` 之外再认 `主文件名-thumb.`,分集剧照
  跟着分集文件走;
- **条目目录级资产随迁**(`_plan_entry_assets` / `_move_entry_assets`):
  条目目录变了就把白名单里的镜像产物一起搬。条目目录的判定复用
  `layout.entry_dir_of`——与镜像写出时是同一个函数,不会出现"镜像写到 A、
  整理去 B 找"的错位;
- **两道守门**(宁可留着也不搬错):旧目录里还留着**不在本次计划里**的
  视频(含季目录下的分集)→ 不搬,图要留给那些文件;同一个旧目录的文件
  搬向多个新条目目录 → 歧义,不搬;
- **白名单**:只认镜像自己写死的那几个文件名(季海报走
  `^season(\d{2}|-specials)-poster\.jpg$`)。用户放进目录的东西一律不碰
  ——代价是那种目录清不掉,这个代价是对的;
- **唯一的删除动作**:目标已存在且**内容逐字节相同**时删掉源头那份重复
  副本(镜像产物随时可由 `data/` 下的资产重建,不是用户数据);内容不同
  则原样保留并告警——那可能是用户自己换过的图。

预览接口新增 `entry_assets`(整理弹窗多一枚统计芯片),整理结论新增
`entry_assets_moved`;Job 计划的序列化/反序列化一并带上,重启恢复不丢。

### 12.3 回归锚与变异验证

五条用例:资产随目录搬且**旧目录彻底消失**(这条是"不留垃圾"的判据)+
再跑幂等;分集剧照随改名;旧目录还有别的视频时不搬;重复副本清掉 / 内容
不同保留并告警;用户自己的文件一律不动。

变异验证五处全红:不搬条目资产、去掉 `-thumb.` 前缀、去掉"旧目录还有别
的视频"守门、目标已存在时直接覆盖、白名单失效(目录里所有文件都搬)。

顺带核过 `library/transfer.py`(转移条目到别的库):它的主路径是**整目录
搬运**(`is_dir=True`),海报/NFO 天然跟着走,不存在同类漏洞;只有"目录里
混着其他条目"的退化路径才逐文件搬,而那种情况目录本就不该被清空。

## 13. 复检：Bug 排查与 CLI / Agent 可用性（2026-08-28）

### 13.1 查出并修掉的三个 Bug

**① `PUT /scrape/config` 是整体替换,一条 CLI 命令抹掉全部配置(严重)**

接口直接收 `MetadataScrapeSetting`,请求体里没给的字段一律按模型默认值
写回去。Web 端总是整体提交,所以从未暴露;但 CLI 与 Agent 天然是"只改一项"
的用法,`_build_body` 也只把显式给出的标志放进 body。实测:

```
配置后：            ['ja-JP','zh-CN'] | {title} ({year}) [tmdbid-{tmdb_id}] | False
只改 poster_mode： []                | ''                                  | True
```

用户精心配好的语言优先级和命名模板被一条 `mclaw scrape set --poster-mode
language` 悄悄清空,接口还回 200——下次整理才发现文件名全变了。

改为**局部更新**:`payload.model_dump(exclude_unset=True)` 合并到当前值,
再走一次完整校验(与 `merge_for_library` 同一套写法)。`model_fields_set`
区分"显式传了默认值"和"根本没传",所以"恢复默认"仍可用显式空值表达。
Web 端整体提交,行为完全不变。

**② 改名全部失败时,条目资产照样被搬走(§12 的漏网之鱼)**

`_plan_entry_assets` 的守门是**计划期**判据("这个目录会不会被搬空"),
管不了执行期改名真的失败(权限不足、并发占用)。实测:

```
renamed: 0  entry_assets_moved: 3
视频还在旧目录: True    海报还在旧目录: False
新目录: ['fanart.jpg','movie.nfo','poster.jpg']   ← 凭空造出的、只有图的目录
```

修法:`_move_entry_assets` 执行前**按源目录再复核一次**——还剩视频就整组
跳过并记一条中文问题。同时补掉一个相关的假告警:作业重跑时旧目录早已被
清掉,此时应静默无事,而不是报"仍有视频文件"。

**③ 域描述漏掉命名模板与目录写入,模型找不到入口**

`mclaw_tool` 的 `scrape` 一行说明只写了"语言/选图/档位",而命名模板和
目录写入开关是这个域的另外两个 tab;后续动作也只提了 `library metadata
refresh`(那是给元数据和图片用的),没提改了命名模板要跑 `library
organize-files`。模型被问"把文件名改成带 tmdbid 的格式"时找不到域,即使
找到了也不知道存量文件要怎么生效。两处域说明与 `scrape.set` /
`organize-files` 的正文一并补写。

### 13.2 CLI 命令面:配套齐全

| 能力 | 命令 |
|------|------|
| 读配置(含"留空时实际生效值") | `mclaw scrape show` |
| 改配置(局部更新) | `mclaw scrape set --xxx` |
| 全量语种/地区表 | `mclaw scrape languages` / `countries` |
| 发现页院线地区 | `mclaw discover region show` / `set --region JP` |
| 让存量文件名生效 | `mclaw library organize-files <id> --dry-run` / `--yes` |
| 库级覆盖 | `mclaw library update <id> --scrape-overrides-json '{...}'` |

### 13.3 真机联调(起真服务 + 真令牌 + 真 CLI)

连打七步验证 Agent 主链路:只改海报模式 → 只改语言优先级 → 只改命名模板
→ 只关 NFO 写入,每步都只动目标字段、其余原样;传非法值时报回
`海报选择只能是 default（TMDB 默认）或 language（按语言优先级）`,且已有
配置零污染。占位符写错的报错还会把该模板可用的占位符全列出来——模型照着
自我纠正一次就对。库级覆盖写非法字段被挡回
`这些设置不支持按库覆盖：poster_mode（选图与语言的产物跨库共享一份…）`。

### 13.4 留下的一处粗糙(不属本次改动,未动)

`mclaw library update` 的 `--name/--kind/--root-paths-json` 是必填,所以
只想加一条库级覆盖也得把这三项一起带上。它是**大声失败**(报"缺少必填
参数"并给出提示),不会静默改坏东西,Agent 先 `library get` 再补齐即可
完成——已实测走通。要彻底顺手就得把 `LibraryPayload` 拆成创建版与更新版,
那是既有接口的语义变更,不在本次范围内。
