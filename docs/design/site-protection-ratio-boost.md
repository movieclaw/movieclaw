# 站点保护与自动刷分享率

PT 站点普遍有上传量 / 分享率考核。一个新接入的站点如果立刻被订阅规则同等对待，
订阅管线会毫无顾忌地从它拉种子——下载量飙高、分享率骤降，轻则被限制下载、
重则封号。本设计提供两个互补机制：

1. **站点保护开关**：打开后该站点不再被订阅链路选中（被动匹配、缺口搜索、
   换源、洗版全部绕开），但用户主动搜索、手动下载完全不受影响。
2. **自动刷分享率（刷流）**：盯着本地种子索引里的免费种子，第一时间抢下做种，
   在用户设定的存储预算内自动汰换上传效率低的、保留高效受欢迎的。

两个机制正交：典型用法是新站「保护 + 刷流」双开，等分享率养起来再关保护。

## 1. 站点保护开关

### 1.1 语义

`SiteCredential.protected`（默认 False）。与 `enabled` / `status` 正交：

| 开关 | 影响面 |
|---|---|
| `enabled=False` | 一切访问停止（搜索、同步、下载） |
| `protected=True` | 只挡**订阅链路**；主动搜索 / 手动下载 / 种子同步照常 |

种子同步（`sync_site_torrents`）**刻意不受保护开关影响**：本地索引是刷流选种
和手动搜索的原料，保护的是"下载量"而不是"浏览页请求"。

### 1.2 实现卡点（两道闸，双保险）

订阅链路触达一个站点只有两条路，各设一道闸：

1. **搜索扇出**：`stream_search_all_sites` 新增 `exclude_protected` 参数，
   订阅侧调用（缺口搜索 `wanted_search`、死种换源 `replacement`）传 True，
   受保护站点直接不发请求（省下的是真实站点压力）。
2. **评估投递**：`evaluate_and_dispatch` 入口过滤受保护站点的种子行。
   这是被动匹配（`sync_site_torrents → process_new_torrents`）的唯一闸门，
   同时兜底任何绕过扇出闸的历史行。

手动链路（`/search/torrents`、手动选种投递 `grab_manual`）不经过这两道闸，
天然不受影响。

## 2. 自动刷分享率

### 2.1 用户视角

站点卡片上一个开关 + 一个预算数字，其余全自动：

- **开关**：`boost_enabled`（默认关）。
- **预算**：`boost_budget_bytes`（默认 100 GiB）。刷流占用的磁盘空间上限，
  引擎在预算内自动汰换。

### 2.2 选种（抢什么）

数据源是本地种子索引 `site_torrent`。**有准入能力时，索引同步钉在系统
最快节奏**——这是动态的，不是"开了刷流就一直快"：

- 统一判据是**准入余量** = 剩余预算 + 可汰换容量（`admission_headroom`）。
  余量 ≥ 1 GiB 时同步钉在 5 分钟下限、不随冷清放疏（抢免费种拼的就是
  发现速度）；预算满且换不动时，发现新种也没机会下，高频刷索引纯属浪费
  站点请求——同步自动回落到正常的冷热自适应/指数退避，准入扫描同样跳过；
- 余量恢复（汰换解锁、调大预算、任务转出/删除）后自动重新钉住；引擎
  发现余量恢复而游标还在慢排期（15 分钟开外）时会推一把让它立即同步，
  不等旧排期走完；
- 开启刷流的瞬间游标立即到期，≤2 分钟内完成首刷；失败退避永远不受钉住
  影响（对挂掉的站高频重试没有意义）。

每 tick 按站点扫描候选，全部条件必须满足：

- `is_free = True`（下载系数 0，下载量零成本——这是刷流不伤分享率的前提）；
- 易变层观测在 2 小时内（`volatile_refreshed_at`）：免费/做种数来自索引
  快照，观测太旧时促销可能已结束，宁可等下一轮同步再看；
- `publish_time` 在 24 小时内（新种的 peer 群最活跃，"第一时间抢"）；
- `leechers ≥ 1`（没有下载者就没有上传对象）；
- `hit_and_run` 不为 True，**除非站点配置了真实考核时长**（站点 YAML 的
  `hr_seed_hours`，规则页明示的政策事实）：考核要求已知才敢碰明确标注
  H&R 的种子，其保留期按真实时长保底（见 2.4）；未知时绝不碰；
- `free_deadline` 为 NULL 或剩余窗口 ≥ max(2h, 按 5 MiB/s 估算的下载时长)
  （免费期内下不完就会产生真实下载量，宁可放弃）；
- `size_bytes` 已知且 ≤ 预算的 1/4（单种吃掉大半预算会让汰换失去弹性）。

**拥堵感知**：准入前先看目标下载器——已有任务在排队（`queued`，说明
队列上限的活动位满了）就本轮暂停投放，队列消化后自动恢复。预算越大越
需要这个闸：没有它，引擎会按自己的节奏把下载器队列塞爆，排队任务吃掉
免费窗口、48 小时后被止损删掉，全程空转。判定用本 tick 已缓存的全量
任务列表，零额外请求。存量排队任务 48 小时仍抢不到活动位时由止损让出
预算（原因如实标注"活动位已满"，不误报死种）。

评分 = `leechers / (seeders + 1)`，再乘上传系数（2x 上传翻倍）。分数高
= 供不应求 = 上传效率预期高。**评分刻意不除以体积**：每个下载者都要下完
整个体积，剩余需求 ≈ L×size，做种者份额 ≈ L×size/(S+1)，除以占用的
size 后每字节期望回报 ≈ L/(S+1)——体积恰好消掉。小体积的真正优势是
敏捷性（下得快、免费窗口风险小、汰换颗粒度细），体现在**同分小者优先**
的排序决胜上，不在评分里重复计价。每站每 tick 最多提交 3 个（对站点克制）。

### 2.3 台账与所有权

新表 `ratio_boost_task`（infohash 唯一）记录每个刷流任务。**所有权铁律**：
提交时下载器返回 `already_exists=True`（用户自己已在下）的种子**不入台账、
永不自动删除**——与订阅管线 `owned_by_movieclaw` 同一哲学。

刷流下载与媒体下载彻底隔离：

- 分类 `movieclaw-boost`（订阅/手动是 `movieclaw`），下载器里一眼可辨；
- 保存目录用下载器默认目录下的 `movieclaw-boost/` 子目录，不落媒体库、
  不进监听导入规则的视野，免费种什么内容都有，不能污染入库识别清单。

### 2.4 效率追踪与汰换（留什么）

**效率的衡量单位是「周转」= 单位存储的上传速度（rate/size），等价说法：
按当前速度，多少天能把自己的体积再上传一遍。** 依据：预算约束的是
存储×时间，最大化总上传是个背包问题，按密度（rate/size）贪心保留/汰换
即近似最优。绝对速度会留错资产——200 GiB 跑 10 KiB/s（周转 240 天）远差
于 2 GiB 跑 8 KiB/s（周转 3 天），前者占着 1/4 预算几乎不产出。

每 tick 从下载器 `list_torrents` 读一次全量（无逐任务请求），按 infohash
对回台账：

- 累计上传量差分 → **时距感知的上传速度 EMA**（α = 1−e^(−dt/W)，等效
  观测窗口 W=24 小时）。窗口以「天」为尺度是刻意的：PT 上传按昼夜周期
  突发，短窗口会把昨晚狂传、白天安静的种子误判成死种。首次观测到下载
  完成时记下 `completed_at`——汰换判定窗口以它起算。
- 台账里有、下载器里没有 → 用户手动删了，标记 `missing` 让出预算，不追究。

**双信源**：实测周转（EMA）说"过去传得快不快"，**蜂群快照**说"未来还有
没有人要"——下载器的任务列表原生携带 tracker 汇报的全网 seeder/leecher
数（qB num_complete/num_incomplete、Tr trackerStats），每 tick 随对账刷进
台账，零额外请求。

**汰换规则**（预算不足以容纳新候选时触发，`evictable`）：

- 前置条件：下载已完成、**入池已满保留期**、周转周期超过 10 天（单位
  存储产出过低）；
- 过了保留期后，判定窗口从**完成时刻**（`completed_at`）起算——完成前
  是在下载，谈不上"传得慢"——按证据强度分层，越确凿越早放行：
  1. **蜂群已死**（tracker 汇报 0 下载者）：未来注定零产出，立即可换；
  2. **零产出速汰**（完成 6 小时累计上传仍不足 64 MiB）：下完却根本
     起不来的种子不必陪跑公平测量窗，及时淘汰腾位；
  3. **公平判定窗**（完成满 12 小时）：EMA 已覆盖足够长的观测（含下载
     阶段的边下边传），周转仍慢就是真慢。
- 汰换顺序：**死种最先走**，其余按周转从慢到快（连数据一起删），腾够空间
  为止；腾不够就放弃新候选，**绝不提前动保留期内的任务**。

**预算收敛**（用户调小了预算时触发，`budget_evictable`）用更宽的判据：
用户显式调小预算是明确指令，占用必须收敛到新预算——只保留"已完成 +
已过保留期"两条铁律，**不设**周转地板与完成后判定窗（高效不是免死
金牌，否则 1000G 调 100G 时高效种子占着 900G 永不归还）。汰换顺序与
上面相同：死种和低效的先走、高效的排最后、只在必要时牺牲；保留期内的
任务不动，等到期后在后续 tick 里继续收敛。前端在用户调小预算保存时
二次确认讲清这些后果。

保留期是 H&R 的安全垫，取两个来源之**大**者：

- 用户配置的 `boost_hold_days`（默认 3 天；0=不保护）——多数站点不提供
  H&R 标记（三态里的 NULL），这是**考核时长未知时的保底**；
- 站点 YAML 的 `hr_seed_hours`（真实考核时长，规则页明示的政策事实）——
  只约束准入时明确标注 H&R 的任务（台账 `hit_and_run` 列）。**解析到
  真实考核要求时以真实为准**：哪怕用户把保底调成 0，H&R 种子也会做满
  考核时长再进汰换候选。

**确认无 H&R 考核的站点可把保留期调到 0 = 自由汰换**——此时分层判定
（完成后 6h/12h 窗口、蜂群已死豁免）仍在，保证的是不误杀刚下完还没测出
效率的种子，而不是 H&R 保护。宁可预算利用率低一点，不能让"自动刷分享率"
反过来制造 H&R 违规。

**未完成任务的止损**（与汰换互补；针对下载中的任务，不受保留期约束——
保留期保护的是已完成的做种，多数站点的 H&R 考核也只针对已完成下载）：

- 免费窗口已过（准入时快照的 `free_deadline`）、剩余还超过 1/10：每多下
  一字节都是付费流量，连数据删除止损；已下到 9 成以上放行下完；
- 下载 48 小时仍未完成：死种/无源，永远占着预算，删除让出。

**站点配置被删除时**：该站在池任务全部转出管理（missing）——预算主体已
不存在，但任务与数据保留继续做种，用户可在下载器里按 `movieclaw-boost`
分类自行处理。

### 2.5 与订阅/手动下载的碰撞（同一种子两条链路都要）

碰撞是双向的，两个方向都有明确处理：

- **订阅/手动先抢、刷流后到**：两层拦截。准入时排除该站已被
  `subscription_download_attempt` / `manual_download_intent` 认领的种子
  （按 site_id + torrent_id）；即便漏网（如认领记录缺 torrent_id），提交时
  下载器幂等返回 `already_exists`，刷流按所有权铁律不纳入管理。
- **刷流先抢、订阅后到**：提交层就地**接管**（`_reclaim_boost_task`）——
  订阅/手动投递发现已存在的任务是刷流引擎自己抢下的，调下载器
  `set_location` 把数据从刷流目录**迁到媒体的目标目录**（入库监听由此
  能看见它；种子往往已下载完成，相当于零流量秒到），台账转出、
  `SubmitResult.reclaimed_from_boost=True`，订阅侧据此把
  `owned_by_movieclaw` 记为 True（本来就是 movieclaw 的任务，享受完整的
  换源/洗版清理状态机）。迁移失败不连累提交：台账保持 active，由每 tick
  的**认领转出**（`hand_over_if_claimed`）兜底所有权——此时文件留在刷流
  目录，订阅按「下载器中已存在的非自有任务」的既有核验/换源路径处理。

### 2.6 与既有机制的关系

- 下载器抽象层补 `TorrentBrief.uploaded_bytes / ratio`（qB `uploaded`、
  Transmission `uploadedEver`），两个适配器同步实现；
- 刷流走 `submit_torrent` 公共编排（默认下载器选择、路径映射守门、站点
  限流器全部复用），只是分类与目录不同；
- 定时任务 `ratio_boost`（INTERVAL 300s）注册进现有调度框架；没有任何
  站点开启刷流时 tick 空转直接返回，零成本。

### 2.7 暂停 / 恢复（临时给前台流量让路）

刷流做种的上行会与用户的前台使用（看视频、远程访问）抢带宽，此前只能整个
关闭刷流——但关闭意味着"不再刷流"，恢复时要重新确认预算，语义太重。
`boost_paused` 是站点级的临时闸：

- **暂停**：在池做种批量压到极低上传限速（每种 1 KiB/s——保住与蜂群/
  tracker 的连接不掉种；刻意不用下载器的"暂停任务"，暂停任务会停止汇报
  tracker，部分站点会记为停止做种，有 H&R 风险），同时引擎停止汰换、
  预算收敛与拉新种，任务与数据全部保留；
- **恢复**：解除上传限速，引擎回到正常节奏。

配套的正确性约束：

- **EMA 冻结**：暂停期间照常记账（完成位/累计量/蜂群快照，止损判定也
  照常——它防的是免费窗口过期后的付费下载），但**不更新上传速度 EMA**：
  限速下的低速不是真实效率，混进 EMA 会在恢复后触发一轮误汰换；
- **落地策略两个方向刻意不对称**：暂停是先落库再下发限速（下发失败由
  引擎每 tick 幂等重发自愈，暂停意图不能因网络抖动丢失）；恢复是先解除
  限速、全部成功才落库（有一台没解除成功就报错保持暂停态，绝不允许
  "界面显示运行中、做种却停在 1 KiB/s"的静默失速）；
- **暂停依附于刷流**：关闭刷流 / 删除站点配置时自动清掉暂停态并尽力解除
  限速；暂停期间索引同步不再钉最快档（不拉新种就没必要高频刷索引）。

### 2.8 明确不做的

- 不做跨站预算分配策略（每站独立预算，用户直接看得懂）；
- 不做魔力/积分感知（各站规则千差万别，声明式 YAML 覆盖不了）；
- 不做主动限速 / peer 择优（下载器自己的事）；
- 不追偿 `missing` 任务（用户手动删除是明确意图）。

## 3. 数据库变更（向前兼容）

- `site_credential` 加三列（均带 server_default，老代码不认识只丢语义不丢数据）：
  `protected`、`boost_enabled`、`boost_budget_bytes`；
- 新表 `ratio_boost_task`：独立新表，回退旧版本时被忽略。

## 4. API

- `PATCH /sites/{site_id}/protection`：`{"protected": bool}`；
- `PATCH /sites/{site_id}/ratio-boost`：`{"enabled": bool, "budget_bytes": int}`；
- `PATCH /sites/{site_id}/ratio-boost/pause`：`{"paused": bool}`——暂停 /
  恢复刷流（语义见 2.7），`ConfiguredSite` 视图相应新增 `boost_paused`；
- `GET /sites/boost-stats`：按 site_id 返回 `{active_count, used_bytes,
  budget_bytes, uploaded_bytes_total, evicted_count, uploaded_bytes_24h,
  avg_used_bytes_24h, uploaded_bytes_7d, avg_used_bytes_7d}`。近期窗口
  指标来自小时桶统计表 `ratio_boost_stat`（每站每小时一行：上传增量 +
  在池占用采样，保留 30 天），站点卡片据此展示过程一句话：
  「近 24 小时：X GB 在池种子贡献了 Y GB 上传 · 近 7 天：…」——比终身
  累计值更能回答"现在还有没有在产出"。

三个端点都返回/配合 `ConfiguredSite` 视图新增的 `protected / boost_enabled /
boost_budget_bytes` 字段。
